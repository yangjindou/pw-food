<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.dou.server.mapper.DisinfectionRecordMapper">

    <select id="getList" resultType="com.dou.server.model.DisinfectionRecord">
        SELECT t.create_date,t.create_user,t.update_date,t.update_user,t.id,w.name warehouse_name,
        t.warehouse,t.area,t.date,t.phone,t.disinfectant_ratio FROM t_disinfection_record t
        left join t_warehouse w on w.id = t.warehouse
        where 1 = 1
        <if test="createUser !=null">
            and t.create_user = #{createUser}
        </if>
    </select>
</mapper>